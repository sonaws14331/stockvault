services:
  - type: web
    name: stockvault
    runtime: node
    plan: free
    buildCommand: cd server && npm install
    startCommand: cd server && node server.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /api/media/categories
    autoDeploy: true
